---
name: probe
description: "生成可独立复用的深度 landscape characterization：穷尽 wiki 内关联论文，梳理研究路线与时间脉络，聚合跨工作的共同缺陷、争议和未知，补缺并输出结构化 probe 文档。适用于宽、新、快速变化或高新颖性风险的研究方向。Triggers on /probe with a topic."
---

# 深度调研 Probe Skill

对给定 topic 做深度 landscape characterization——理解领域的 assumptions、tensions、和空白在哪，输出结构化 probe 文档。**不是 literature review 列表**，而是对领域内已有工作的定位分析 + 未解决问题识别。

## 共享中文写作契约

在生成 probe 正文、表格单元格或 probe log 前，必须完整阅读并执行 [中文写作与术语解释契约](../_shared/chinese-writing.md)。来源论文页或外部资料里的中英混写不得直接复制到 probe。

## 用法

```
/probe <topic or question> [--ingest-missing]
```

- 默认：外部论文只作为 URL evidence，不修改 raw/wiki layer
- `--ingest-missing`：允许下载高相关外部论文并执行 mineru + wiki-paper；同时允许补齐 wiki 内引用但尚未 ingest 的论文

## 执行步骤

### 步骤 1 — 穷尽 wiki 内相关信息

从 `wiki/index.md` 出发：
- 找到相关的 **entity / concept / comparison / theme / conference** 页
- 在每个主题/概念页的「引用本概念的论文」或「相关论文」中顺 wikilink 向下读所有关联的 paper wiki 页
- 对每篇读过的 paper，提取：
  - **做了什么**：一句话核心贡献
  - **没做什么**：什么 scope 明确排除或没碰的
  - **关键观察**：论文依赖的 workload / bottleneck / scaling / deployment observation
  - **隐含假设**：what must be true for this approach to work？（例如 FlexiCache 假设 attention head stability 是 model-intrinsic、DiffKV 假设 attention score 反映持久重要性）
  - **可攻击点 / 脆弱点**：从 `批判性分析`、`局限与后续工作` 提取哪些假设可能被测量推翻

优先读论文页里的 `关键观察 / 隐含假设`、`批判性分析`、`局限与后续工作`。兼容旧页英文栏目；如果旧论文页没有这些节，再从 `核心方法`、`关键结果` 和原始 Markdown 提取。

对每个相关 entity/concept/theme/conference 页也做同样的提取——它们代表了「社区共识」，隐含假设更强。

逐篇抽取是分析材料，不是正文的默认组织方式。完成抽取后，先做跨论文综合：

1. 按「试图解决的核心问题」归纳研究路线，而不是只按论文所属领域或系统组件分类。中等规模主题通常形成 3–7 条；材料很少时可以更少，超过 7 条时应检查能否合并共享同一问题的类别。
2. 为每篇工作指定一条主要路线，并可补充至多两个辅助分面；避免同一工作在正文多处重复介绍。
3. 标注工作在路线中的历史角色：奠基、扩展、转折、反例、评测或产业实现。
4. 为每条路线识别起点、关键转折和当前前沿。时间脉络必须说明后续工作相对前一阶段改变了什么，不能只按年份罗列论文。
5. 建立「缺陷—工作」矩阵，寻找跨工作的重复缺陷。只有满足以下任一条件才能称为「共同缺陷」：
   - 至少出现在 3 篇工作中，并跨越至少 2 条研究路线；
   - 覆盖某一研究路线中至少三分之一的相关工作，且分母明确。

共同缺陷必须说明适用范围、机制性原因、后果、已有缓解或反例以及剩余缺口。证据不足时标为「候选共同缺陷」，不能用「很多论文」等模糊表述。

### 步骤 2 — 补缺

对 probe 过程中发现的：
- wiki 内引用了但缺 paper wiki 页的论文 → 默认列入 coverage gap；传 `--ingest-missing` 时才下载 PDF + 跑 mineru + 用 `/wiki-paper` 生成
- 引用的概念但没有 concept 页且达到 `wiki/.quality.yml` 的 threshold → 仅列为候选，不在 probe 内建页

确保 landscape 覆盖完整后再进入 Step 3。

### 步骤 3 — 外部搜索

- **WebSearch arxiv**：找最新的相关论文（特别是 2025-2026、在 wiki 覆盖范围外的）
- **WebSearch 博客/行业动态**：找如 LMCache blog、NVIDIA developer blog 等非正式但关键的信息源
- **WebSearch 工业系统**：找 closed-source 但公开讨论过的系统（如 NVIDIA KVBM、InfiniStore）
- 对找到的高相关论文：默认作为外部证据引用；只有用户显式传 `--ingest-missing` 才下载并进入 paper ingest

### 步骤 4 — 输出结构化 probe 文档

输出到 `wiki/probes/{Slug}.md`。Probe 是独立研究版图，可以被零个、一个或多个 proposal 复用；不为满足流程强行生成 proposal。

写作前在内部统一术语表达，不默认输出术语表。常用系统缩写直接使用；只有多个非标准术语确实阻碍理解时才增加简短阅读提示。正文优先呈现研究路线、演化和跨工作结论；逐篇的「做了什么」、「隐含假设」和「可攻击点」保留在附录证据卡中。

**文件命名**：Slug = topic 名称，kebab-case（如 `thinking-model-kv-cache`）。

```markdown
---
type: probe
title: "{简洁中文展示标题，不加‘深度调研’前缀}"
topic: {简短 topic 描述}
created: YYYY-MM-DD
last_updated: YYYY-MM-DD
probed_papers: ["[[Page1]]", "[[Page2]]", ...]
---

# 深度调研（Probe）：{Topic}

## 阅读提示

{若全文有超过 5 种反复专业术语，用简短中文解释核心术语、缩写和本 probe 采用的统一表达。}

## 一页结论

- {领域形成了哪些主要研究路线}
- {最近的关键转折是什么}
- {覆盖最广、后果最显著的 2–4 个共同缺陷}
- {哪些结论仍有实质争议}
- {提出 proposal 前最需要测量什么}

## 范围与证据

{用短段落说明纳入和排除范围、证据等级、覆盖数量及主要 coverage gap；详细论文清单留在 frontmatter 和附录。}

## 研究版图

| 研究路线 | 核心问题 | 代表工作 | 当前边界 |
|----------|----------|----------|----------|
| {路线 A} | | [[X]]、[[Y]] | |
| {路线 B} | | [[Z]] | |

{解释分类依据、路线之间的关系，以及这张图揭示的整体格局。}

## 时间脉络

### 总体阶段

{用一条简短阶段链概括领域如何演化，例如：静态知识库 → 可执行基准 → 自动闭环 → 独立审查。}

### {路线 A}

- **YYYY — [[X]]**：{起点或转折；明确它相对前一阶段改变了什么。}
- **YYYY — [[Y]]**：{后续转折。}

{每条路线只保留改变方向、能力边界或评价口径的工作；其余工作放在路线分析和附录。}

## 各类工作的演化

### {路线 A}

**共同目标**：{这一类工作共同解决什么问题。}

**演化与分歧**：{方法如何演变，代表工作在哪些关键选择上不同。}

**共同边界**：{这一类整体没有解决什么。}

**相关工作**：[[X]]、[[Y]]、[[Z]]

{其余路线使用相同结构。}

## 跨工作共同缺陷

| 共同缺陷 | 覆盖范围 | 为什么是系统性问题 | 已有缓解与剩余缺口 |
|----------|----------|--------------------|--------------------|
| {缺陷 A} | {涉及 N/M 篇工作、哪些路线} | | |

{按覆盖面、后果严重性和证据强度排序。每项列出反例或部分解决者；未达到门槛的内容标为候选。}

## 关键争议

{只保留不同工作给出冲突结论、或结论明显依赖工作负载与部署条件的地方。每项说明争议双方、证据边界和可能的条件化解释。单篇工作的局限进入附录，不在这里重复。}

## 产业实践与学术缺口
{工业界在做什么但没发论文的？列出已知的闭源系统、博客和技术演讲。}

## 候选空白
{可能的空白——不是研究点子，是「这里似乎没人碰过」的位置。每项必须回溯到研究路线的断点、共同缺陷或关键争议，并解释现有工作为什么没有覆盖。}

## 关键未知与测量
{做任何提案之前需要搞清楚的问题。每项给出最小可行测量，说明所需工作负载、轨迹、指标和可推翻的结论。}

{章节分工：重复出现且已有证据的问题进入「共同缺陷」；不同工作结论冲突的内容进入「关键争议」；尚无人覆盖的位置进入「候选空白」；需要测量才能判定的内容进入「关键未知与测量」。同一判断只在一处完整展开，其他位置用一句话交叉引用。}

## 覆盖缺口

{列出缺 paper wiki、证据等级不足、只有外部摘要或尚无公开资料的部分。}

## 附录：逐篇证据卡

### [[X]]

- **路线与角色**：{主要路线；奠基/扩展/转折/反例/评测/产业实现。}
- **证据等级**：{complete/full-text、abstract-only、needs-review 等。}
- **做了什么**：{一句话核心贡献。}
- **没做什么**：{明确排除或未覆盖的范围。}
- **关键观察**：{workload / bottleneck / scaling / deployment observation。}
- **隐含假设**：{该方法成立必须为真的条件。}
- **可攻击点 / 脆弱点**：{可被什么测量验证或推翻。}

{每篇相关工作一张卡，按主要研究路线分组；外部论文使用 Markdown 链接。}
```

### 步骤 4a — 成稿语义审计

在追加 log 前，按共享契约审查表格单元格、研究版图总结、时间脉络、共同缺陷、争议和测量建议。消除英文名词串、未解释的非标准缩写和常用缩写的冗余展开，再运行定向 `wiki-lint --language-only`；`language_warnings=0` 不能替代语义审计。

### 步骤 5 — 追加 wiki/probes/_log.md

```markdown
## [YYYY-MM-DD] 调研：{Topic}
- 生成：[[{Slug}]]
- 覆盖 {N} 篇论文，{M} 个候选空白，{K} 个关键未知问题
```

---

## 关键约束

- **不做新颖性判断**，不生成研究点子。probe 是中立的研究版图分析
- **穷尽 wiki**：至少读完 wiki/index.md 中所有相关 page 的第一层 wikilink 链路
- **逐篇抽取不等于正文结构**：必须先归纳研究路线、时间转折和共同缺陷；不能用逐篇宽表代替跨论文综合
- **必须填完整证据卡**：每篇工作保留「做了什么」「没做什么」「关键观察」「隐含假设」「可攻击点 / 脆弱点」，作为综合结论的可追溯依据
- **正文表格最多四列**：论文级信息使用分组证据卡；不以横向宽表作为研究版图入口
- **时间轴只保留转折点**：必须说明每个节点改变了什么，不收录仅仅时间较新的工作
- **共同缺陷必须可核查**：给出适用论文数、相关路线、分母或门槛、反例与证据边界
- **优先利用新版论文页**：已有论文页的 `关键观察 / 隐含假设`、`批判性分析`、`局限与后续工作` 是 probe 的主要原料；缺节时才回 Markdown/PDF
- Probe 的中文叙述、术语解释和可读性必须通过共享写作契约；论文、系统、模型和指标原名按契约保留并首次解释。
- **证据分级**：`complete/full-text` 可作强证据；`abstract-only` 只能作线索，关键论断必须回源；`needs-review` 不得单独支撑共同缺陷、关键争议或候选空白
- **probe 文档不引用自身到 wiki/index.md**，它是 `wiki/probes/` 下的独立研究版图
- **probe 的 probed_papers 列表是 wikilink**（指向 wiki paper 页），外部论文用 markdown link 到 arxiv URL
- 新建 probe 时 `created` 与 `last_updated` 相同；刷新现有 probe 时保留 `created`，只更新 `last_updated`，供 proposal 判断证据时效
- 补缺时（Step 2）顺带完成论文下载与 wiki 生成，不阻塞 probe 主体输出
